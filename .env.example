# ─── Development only ────────────────────────────────────────────────────────
# In Docker containers RUNTIME_BFF_URL takes precedence at runtime via
# window._env_ (injected by env.sh on container start).

# BFF URL  →  Docker: RUNTIME_BFF_URL
VITE_BFF_URL=http://localhost:9003
